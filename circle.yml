machine:
  node:
    version: 6.9.1

dependencies:
  pre:
    - npm install -g npm

test:
  override:
    - echo 'test'

compile:
  override:
    - npm run site
  post:
    - bash ./build/afterBuild-2.0.sh
